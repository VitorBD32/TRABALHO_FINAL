<p style="color: green"><%= notice %></p>

<h1>Funcionarios</h1>

<div id="funcionarios">
  <% @funcionarios.each do |funcionario| %>
    <%= render funcionario %>
    <p>
      <%= link_to "Show this funcionario", [:administrativo, funcionario] %>
    </p>
  <% end %>
</div>

<%= link_to "New funcionario", new_administrativo_funcionario_path %>
